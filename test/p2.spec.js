/*!
 * P2 unit tests
 */

/**
 * Module Dependencies
 */

var test = require('tape'),
    P2 = require('../index');

/**
 * Setup
 */

var setup = function (t) {
};

/**
 * Teardown
 */

var teardown = function (t) {
};

/**
 * P2 Class
 */

test('P2 should be a class', function (t) {
    t.plan(1);
    t.ok(P2, 'class should exist');
});